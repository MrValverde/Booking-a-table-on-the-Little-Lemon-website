const CallToAction = ({ primaryText, secondaryText, description, cta }) => {
  return (
    <section>
      <div className='cta-header'>
        <h1 className='display'>{primaryText}</h1>
        <h2>{secondaryText}</h2>
      </div>
      p.description
    </section>
  );
};
export default CallToAction;
